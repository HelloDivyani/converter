converter is a unit converter android app named as Easy Converting 
which helps user to easily convert quantites such as pressure,temperature
distance,time,energy into their different commonly used units.

how it works? -user need to select the quantity to be converted and
provide input data and also select unit.
then on clicking the convert button all possible conversions are displayed.

Thus, helps user to convert units easily.